{
  "name": "web2",
  "version": "1.0.0",
  "description": "Agent-first CLI browser automation tool — runs entirely in Docker. Also a Claude Code plugin.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:cli": "cd cmd/web2 && go build -o web2 .",
    "test": "tsx --test 'src/tests/**/*.test.ts'",
    "test:e2e": "./e2e/run.sh",
    "test:docker": "./e2e/run.sh --docker"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "commander": "^15.0.0",
    "linkedom": "^0.18.13",
    "playwright": "^1.63.0",
    "tsx": "^4.23.13"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^5.8.0"
  }
}
